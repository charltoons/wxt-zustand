import { describe, expect, test } from 'bun:test'

describe('wxt-zustand', () => {
  test('package exports are available', () => {
    // This is a placeholder test to ensure the test framework is working
    expect(true).toBe(true)
  })
})
